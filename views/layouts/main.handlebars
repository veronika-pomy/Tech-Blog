<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" type="text/css" href="/css/normalize.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
    <header>
        <nav>
            <a href="/">Home</a>
            {{!-- navigates to homepage with existing blog posts that include the post title and the date created --}}

            <a href="/">Dashboard</a> 
            {{!-- navigates to space with my blog posts and option to add a new blog post --}}

            {{#if loggedIn}}
            <button id="logout" class="btn-no-style">Logout</button>
            {{else}}
            <a href="/login">Login</a>
            {{/if}}
        </nav>

        <h1>The Tech Blog</h1>
        
    </header>

    <main>
        <div class="post">
            {{{body}}}
        </div>
    </main>

    {{#if loggedIn}}
        <script src="/js/logout.js"></script>
    {{/if}}

</body>